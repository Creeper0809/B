// v2 library: Slice helpers
//
// Intended for v2-compiled output binaries.

import std.mem;

struct Slice {
	ptr: u64;
	len: u64;
};

func slice_eq(a_ptr, a_len, b_ptr, b_len) {
	if (a_len != b_len) {
		return 0;
	}
	return memeq(a_ptr, b_ptr, a_len);
}
