// Test: Basic struct definition and usage
// Expect exit code: 0

struct Point {
    x: u64;
    y: u64;
}

func main(argc, argv) {
    // TODO: 구조체 변수 선언 및 초기화
    // var p: Point;
    // p.x = 10;
    // p.y = 20;
    
    return 0;
}
