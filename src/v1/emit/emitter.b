// v1 emitter
// Roadmap: docs/roadmap.md (4단계)
// Depends on: std (sys_write/sys_open etc), Slice
// Planned:
// - emit_init()
// - emit_str(ptr, len)
// - emit_u64(x)
// - emit_flush(fd)
// - emit_to_file(path)
