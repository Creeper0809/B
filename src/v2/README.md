# v2

`src/v2/`는 v1 위에 기능을 쌓는 **분리된 v2 프론트엔드/코드젠** 작업 공간입니다.

원칙:
- v1에서 이미 안정화된 동작은 v2에서도 회귀 테스트로 유지합니다.
- v2 구현이 시작되면, 초기에는 `src/v1/std`, `src/v1/core`, `src/v1/emit` 등 **공유 가능한 런타임/유틸을 그대로 재사용**하고,
  문법/의미가 갈라지는 지점부터 `src/v2/lex`, `src/v2/parse`, `src/v2/driver`를 중심으로 교체하는 방향을 권장합니다.
