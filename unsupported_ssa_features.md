# v3_20: AST Codegen vs. SSA Codegen 기능 비교

`v3_20/src/codegen.b`와 `v3_20/src/ssa/codegen.b`의 코드를 분석하여, 기존 AST 코드 생성기에서는 지원하지만 새로운 SSA 기반 코드 생성기에서는 아직 구현되지 않았거나 제한적으로 지원되는 기능 목록입니다.

SSA 코드 생성기로 컴파일할지 여부는 `ssa_codegen_is_supported_func` 함수에서 결정합니다. 이 함수가 0을 반환하면, 해당 함수는 기존 AST 코드 생성기(폴백)를 통해 컴파일됩니다.

## SSA 코드 생성기에서 미지원/제한되는 기능

### 1. 표준 라이브러리 함수
- 함수 이름이 `std_`로 시작하는 모든 함수는 SSA 코드 생성 대상에서 명시적으로 제외됩니다.

### 2. 크기가 큰 구조체(Large Struct) 처리
- **16바이트를 초과하는 구조체를 값으로 반환하는 함수**:
  - 함수 호출, 함수 포인터 호출, 대입문 등 대부분의 상황에서 지원되지 않습니다.
  - 기존 AST 코드 생성기는 반환 값 저장을 위한 숨겨진 포인터를 인자로 넘기는 방식으로 이 문제를 처리하지만, SSA 코드 생성기에는 이 로직이 아직 완전히 구현되지 않은 것으로 보입니다.

### 3. 특정 연산자
- **단항 연산자(Unary Operator)**:
  - `~` (Bitwise NOT) 연산자는 지원되지 않습니다. (`-`, `!`만 지원)
- **이항 연산자(Binary Operator)**:
  - `+`, `-`, `*`, `/`, `%`, `^`, `&`, `|`, `<<`, `>>`, `==`, `!=`, `<`, `>`, `<=`, `>=`, `&&`, `||` 외의 다른 이항 연산자는 지원되지 않습니다.

### 4. 복잡한 대입문
- `AST_ASSIGN` 노드 처리 로직에 여러 제약 조건이 존재합니다. 특히 크기가 큰 구조체를 반환하는 함수 호출 결과를 바로 변수에 대입하는 경우 제한이 있을 수 있습니다.

### 5. 일부 함수 매개변수 타입
- `TYPE_SLICE` 등 특정 타입의 매개변수를 처리하는 로직이 있으나, 모든 경우를 완벽하게 지원하지 않을 수 있습니다.

### 6. 크기가 0인 구조체
- `sizeof_type` 함수가 0을 반환하는, 즉 크기가 0인 구조체를 사용하는 함수는 SSA 코드 생성기에서 지원되지 않습니다.

이 목록은 `ssa/codegen.b` 파일의 `ssa_codegen_is_supported_func`, `_ssa_codegen_stmt_supported`, `_ssa_codegen_expr_supported` 함수들의 로직을 기반으로 작성되었습니다.
